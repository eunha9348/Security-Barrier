# Security Barrier — 복붙용 프롬프트 모음

Claude Code에서 이 스킬을 실제로 만들 때 **그대로 복사해서 붙여넣는** 프롬프트입니다.
설계 근거와 조사 결과는 `docs/Security-Barrier-Design.pdf` 를 보세요.

- 대상 스킬 호출명: `/security-barrier`
- 작성일: 2026-09-13

---

## 사용 순서

| 단계 | 프롬프트 | 산출물 |
|---|---|---|
| 0 | [P0. 공통 규칙](#p0-공통-규칙-항상-먼저-붙일-것) | 매 프롬프트 앞에 붙임 |
| 1 | [P1. 스킬 골격](#p1-스킬-골격-생성) | `SKILL.md`, 디렉터리 |
| 2 | [P2. Security Axis 스키마](#p2-security-axis-스키마) | `axis/security-axis.yaml` |
| 3 | [P3. 1차 판정기](#p3-1차-판정기-결정론적) | `scripts/classify.py` |
| 4 | [P4. 양방향 추적](#p4-양방향-추적) | `scripts/trace.py` |
| 5 | [P5. 폐기와 검증](#p5-폐기와-검증) | `scripts/purge.py`, `verify.py` |
| 6 | [P6. 카나리](#p6-카나리-등록과-탐지) | `scripts/canary.py` |
| 7 | [P7. 출력측 억제](#p7-출력측-억제-suppress) | SUPPRESS 단계 |
| 8 | [P8. 리포트 스키마](#p8-리포트-스키마) | `references/report-schema.md` |
| 9 | [P9. 자체 검증](#p9-자체-검증) | 테스트 |
| 10 | [P10. 플러그인 포장](#p10-플러그인으로-포장-선택) | 배포 단위 |

---

## P0. 공통 규칙 (항상 먼저 붙일 것)

```
아래 작업 전체에 적용되는 절대 규칙이다. 위반 시 작업을 중단하고 보고하라.

1. 확인하지 않은 사실을 쓰지 마라. API 파라미터, 프론트매터 필드, 라이브러리 동작을
   기억에 의존해 단정하지 말고, 공식 문서를 확인한 뒤 근거 URL을 함께 남겨라.
   확인 불가능하면 코드에 TODO(unverified) 주석을 달고 나에게 보고하라.
2. 이 스킬은 "LLM이 잊게 만드는" 스킬이 아니다. 모델 가중치와 제공자 서버 로그는
   범위 밖이다. 코드·문서·출력 어디에도 "모델이 잊었다", "완전 삭제됨" 같은
   검증 불가능한 표현을 쓰지 마라.
3. 판정과 삭제는 반드시 증거를 동반하라. 파일 경로, 라인 번호, SHA-256 해시 중
   최소 하나가 없으면 "탐지됨"도 "삭제됨"도 출력 금지다. 대신 UNVERIFIED로 표기하라.
4. 삭제는 검증으로 닫는다. 삭제 후 동일 탐지기를 재실행해 0건을 확인하고,
   삭제 전후 해시를 기록하라. 검증에 실패하면 "삭제됨"으로 보고하지 마라.
5. 절대 추적할 수 없는 영역(제공자 서버 로그, 타인의 세션, 이미 외부로 전송된 데이터)은
   리포트 말미에 항상 명시적으로 열거하라. 조용히 생략하지 마라.
6. 크리덴셜은 삭제로 해결되지 않는다. 탐지되면 "노출 가정 + 회전 필요"로 처리하라.
```

---

## P1. 스킬 골격 생성

```
Claude Code 스킬을 새로 만든다. 이름은 security-barrier 이고 /security-barrier 로 호출한다.

목적: 사내 보안 자료가 Claude Code 세션에 유입됐을 때, 유입 경로를 역추적하고
유입 이후 생성된 파생물을 정추적해서, 파생물부터 원본 순으로 폐기하고,
폐기됐음을 검증 가능한 형태로 증명하는 것.

먼저 https://code.claude.com/docs/en/skills 를 읽고 현재 지원되는 프론트매터 필드와
디렉터리 규칙을 확인하라. 기억에 의존하지 마라.

확인 후 아래 구조로 스캐폴딩하라:

.claude/skills/security-barrier/
  SKILL.md
  axis/security-axis.yaml
  scripts/classify.py
  scripts/trace.py
  scripts/purge.py
  scripts/verify.py
  scripts/canary.py
  references/report-schema.md

SKILL.md 요구사항:
- 1차 판정은 모델이 아니라 스크립트가 한다. classify.py 실행 결과를
  스킬 본문이 모델에 도달하기 전에 주입하는 문법을 문서에서 확인해 사용하라.
- 주입된 결과는 "스크립트가 산출한 사실"이며 재해석 금지임을 본문에 명시하라.
- hit이 0건이면 즉시 중단하고 미검출로 보고하도록 분기하라.
- allowed-tools 로 scripts/ 하위 실행을 사전 승인하라.
- 폐기 작업 중 외부 전송 계열 툴을 disallowed-tools 로 제거할지 검토하고,
  그 판단 근거를 주석으로 남겨라.
- 추적 작업을 서브에이전트에서 돌릴지(context: fork) 검토하라.
  장점(보안 내용이 메인 세션 컨텍스트에 안 들어감)과 한계(서브에이전트 전사도 전사임)를
  둘 다 SKILL.md 주석에 적어라.

스크립트는 아직 비워두고 골격과 인터페이스만 만들어라. 내용은 다음 프롬프트에서 채운다.
```

---

## P2. Security Axis 스키마

```
axis/security-axis.yaml 을 설계하라. 이것은 이 스킬의 1차 판단 기준이며,
회사의 인가된 사용자만 수정할 수 있어야 한다.

필수 요구사항:
- 에이전트는 이 파일을 읽을 수만 있고 수정할 수 없어야 한다.
  파일 권한, CODEOWNERS, pre-commit 훅 중 무엇으로 강제할지 제안하고 근거를 달아라.
- 서명 검증(GPG 또는 sigstore) 필드를 스키마에 포함하라.
  서명이 검증되지 않으면 스킬이 실행을 거부하도록 설계하라.

스키마에 포함할 섹션:
1. classification: 보안 등급 정의. 각 등급마다 탐지 규칙과 대응 액션을 붙인다.
2. patterns: 결정론적 탐지 규칙. 정규식, 파일 경로 글롭, 문서 해시 목록.
   정규식은 반드시 테스트 케이스를 동반해야 한다.
3. canaries: 카나리 문자열 등록부. 심은 위치와 등록일 포함.
4. exclusions: 오탐이 확인된 항목. 제외 사유와 승인자 필수.
5. unreachable: 이 스킬이 도달할 수 없는 영역 목록. 리포트에 그대로 출력된다.

각 필드에 주석으로 "왜 이 필드가 필요한가"를 한 줄씩 달아라.
```

---

## P3. 1차 판정기 (결정론적)

```
scripts/classify.py 를 구현하라.

가장 중요한 제약: 이 스크립트에는 LLM 호출이 일절 없어야 한다.
정규식, 해시 비교, 경로 매칭, 엔트로피 계산만 사용하라.
판정의 재현성이 이 스킬 전체의 신뢰 근거다.

입력:
  --axis PATH        security-axis.yaml 경로
  --scope PATH...    스캔 대상 경로 (기본: 워크스페이스 전체)

동작:
1. axis 파일의 서명을 먼저 검증한다. 실패하면 exit 2 와 함께 중단한다.
2. 스캔 대상에서 axis의 patterns 에 매칭되는 항목을 찾는다.
3. 매칭마다 다음을 기록한다: 파일 경로, 라인 번호, 매칭된 규칙 ID,
   매칭 문자열의 SHA-256(원문은 절대 출력하지 않는다), 보안 등급.

출력: JSON. 스키마는 아래를 반드시 지켜라.
{
  "axis_signature": "verified" | "failed",
  "scanned_at": ISO8601,
  "scope": [...],
  "hits": [
    {"rule_id":..., "path":..., "line":..., "sha256":..., "classification":...,
     "confidence": "deterministic"}
  ],
  "unreachable": [...],
  "errors": [...]
}

절대 금지:
- 탐지된 원문을 출력에 포함하지 마라. 해시만 남겨라. (출력이 곧 새로운 유출이다)
- "아마도", "가능성 있음" 같은 추정 등급을 만들지 마라. 매칭되거나 아니거나 둘 중 하나다.

pytest 테스트를 함께 작성하라. 정탐 케이스와 오탐 케이스를 각각 최소 3개씩 포함하라.
```

---

## P4. 양방향 추적

```
scripts/trace.py 를 구현하라. classify.py 의 JSON 출력을 입력으로 받는다.

두 방향을 독립적으로 수행하고 결과를 합친다. 서로의 결과에 의존하지 마라.

REVERSE TRACE (역추적) — 이 자료가 어디서 들어왔나:
- 어느 파일이 언제 워크스페이스에 들어왔는지 (git log, 파일 mtime)
- 어느 커밋이 처음 도입했는지 (git log -S 로 문자열 도입 커밋 추적)
- 확인 불가능한 유입 경로는 UNKNOWN 으로 남기고 추측하지 마라

FORWARD TRACE (정추적) — 들어온 뒤 무엇을 낳았나:
- 해당 내용을 포함하는 다른 파일 (해시/부분문자열 매칭)
- 해당 파일을 건드린 이후 커밋
- 빌드 산출물, 캐시, 로그 디렉터리
- 에이전트 워크스페이스 산출물: 세션 전사, CLAUDE.md, 메모리 파일, 툴 로그
  ※ 이 경로들이 현재 환경에서 실제로 어디에 있는지 먼저 확인하고,
    확인 안 되면 TODO(unverified) 로 표시한 뒤 나에게 보고하라. 추측 경로를 하드코딩하지 마라.

출력: 각 노드에 다음을 붙인 그래프 JSON.
- 증거 종류 (git-log | file-hash | mtime | content-match)
- 증거 원문 위치 (커밋 해시, 파일 경로, 라인)
- 증거가 없으면 노드를 만들지 마라

참고 개념: 데이터 리니지의 backward(근본원인) / forward(영향분석) 분석을
에이전트 워크스페이스에 적용한 것이다. 새로운 알고리즘을 발명하려 하지 마라.
```

---

## P5. 폐기와 검증

```
scripts/purge.py 와 scripts/verify.py 를 구현하라.

purge.py 순서 (이 순서를 지켜라):
1. 파생물 먼저, 원본 나중. 원본을 먼저 지우면 파생물 추적 근거를 잃는다.
2. 삭제 전 각 대상의 SHA-256 과 크기를 기록한다.
3. 삭제를 수행한다. git 히스토리 재작성이 필요하면 git-filter-repo 를 쓰되,
   반드시 --dry-run 결과를 먼저 보여주고 나의 승인을 받아라. 무단 실행 금지.
4. tombstone 을 기록한다. 원본 식별자, 삭제 사유, 삭제 시각, 승인자, 삭제 전 해시.
   tombstone 자체에는 보안 내용을 넣지 마라.
5. 크리덴셜로 분류된 항목은 삭제 대상이 아니라 "회전 필요" 목록으로 분리한다.

verify.py:
1. purge 후 classify.py 를 동일 인자로 재실행한다.
2. hit 이 0 이 아니면 실패다. 남은 hit 을 그대로 보고하라.
3. 삭제 전후 해시를 대조해 의도한 대상만 지워졌는지 확인한다.
4. 검증에 실패했는데 "삭제 완료"로 보고하는 코드 경로가 존재하면 안 된다.
   구조적으로 불가능하게 만들어라.

tombstone 설계 참고: MemLineage (arXiv:2605.14421) 가 비파괴 삭제 + 포함 증명 유지를 위해
tombstone 마커를 쓴다. 해당 논문의 접근을 확인한 뒤 차용 여부를 판단하라.
```

---

## P6. 카나리 등록과 탐지

```
scripts/canary.py 를 구현하라.

목적: 가짜 보안 문서/크리덴셜을 의도적으로 심어두고, 그것이 모델 출력이나 외부 채널에
나타나면 유출 확정 신호로 쓴다. 심은 곳 외에는 존재할 수 없는 문자열이므로
구조적으로 오탐이 0이다.

기능:
  --generate   카나리 문자열 생성. 충분한 엔트로피, 우연 충돌 불가능해야 함.
  --register   axis 의 canaries: 섹션에 등록 (심은 위치, 등록일, 담당자)
  --scan       주어진 텍스트/파일에서 등록된 카나리 검출

주의:
- 카나리는 은폐 수단이 아니라 탐지 수단이다. 진짜 데이터를 감추는 용도로 쓰지 마라.
- 카나리 문자열 자체가 로그에 평문으로 남지 않도록 설계하라.
- 직접 매칭이 실패하는 경우(패러프레이즈, 번역, 부분 추출)가 있음을 문서화하라.
  이 한계를 숨기지 마라.
```

---

## P7. 출력측 억제 (SUPPRESS)

```
파이프라인에 SUPPRESS 단계를 추가하라. PURGE 로 제거할 수 없는 잔존분에 대한 2차 방어다.

접근: CURE (arXiv:2509.25973, Corrective Unlearning with Retrieved Exclusions) 방식.
먼저 해당 논문을 확인하고, 아래 이해가 맞는지 검증한 뒤 구현하라:
- 경량 corrector 가 출력에 누출이 있는지 검증하고, 있으면 재작성한다
- 제외 대상을 검색해 in-context 레퍼런스로 corrector 에 제공한다
- 그래서 추가 학습 없이 신규 제외 요청에 적응한다

보강 근거: Guardrail Baselines for Unlearning in LLMs (arXiv:2403.03329) 는
프롬프트/입력필터/출력필터 같은 단순 가드레일이 파인튜닝 언러닝과 대등한 성능을 낸다고 보고한다.

절대 지킬 것:
- SUPPRESS 의 결과는 "차단"이 아니라 "완화"로만 보고하라. CURE 도 100%가 아니다.
- "이제 모델이 이 내용을 말할 수 없습니다" 같은 문장을 출력하지 마라.
```

---

## P8. 리포트 스키마

```
references/report-schema.md 를 작성하고, 스킬이 항상 이 스키마로만 보고하게 하라.

필수 섹션:
1. 판정 요약 — 탐지 건수, 등급별 분포. 원문 없이 해시만.
2. REVERSE TRACE 결과 — 유입 경로. 증거 없는 노드는 UNKNOWN.
3. FORWARD TRACE 결과 — 파생물 목록. 증거 종류 명시.
4. 폐기 결과 — 대상별 상태: PURGED_VERIFIED / PURGE_FAILED / UNVERIFIED
5. 회전 필요 목록 — 크리덴셜. 삭제로 해결 안 됨을 명시.
6. SUPPRESS 적용 범위 — "완화" 표현만 사용.
7. 도달 불가 영역 — 항상 출력. 비어 있으면 안 된다.
   최소 포함: 제공자 서버측 로그, 타인의 세션, 이미 외부로 전송된 데이터,
   이 워크스페이스 밖의 사본.

상태값은 위 5개 외에 만들지 마라. "아마 삭제됨", "대부분 처리됨" 같은 값 금지.
```

---

## P9. 자체 검증

```
지금까지 만든 스킬 전체를 검증하라.

1. 합성 테스트 픽스처를 만들어라. 진짜 보안 자료를 절대 쓰지 마라.
   가짜 문서, 가짜 API 키 형식 문자열, 가짜 등급 라벨로 구성하라.
2. 전체 파이프라인을 픽스처에 대해 실행하고 각 단계 출력을 보여라.
3. 다음 실패 케이스를 의도적으로 만들고 스킬이 올바르게 실패하는지 확인하라:
   - axis 서명 검증 실패 → 실행 거부되는가
   - purge 후에도 hit 이 남음 → "삭제됨"으로 보고하지 않는가
   - 추적 증거 없음 → UNKNOWN 으로 남기는가, 추측하지 않는가
   - 도달 불가 영역 섹션이 비어서 출력되는 경로가 있는가
4. 스킬 본문과 스크립트 전체에서 다음 표현을 grep 하고, 나오면 전부 제거하라:
   "잊", "forget", "완전 삭제", "완전히 제거", "차단됨", "안전합니다"
5. P0 의 6개 규칙 각각에 대해 어디서 어떻게 강제되는지 표로 정리하라.
   프롬프트 문구로만 강제되고 코드로 강제되지 않는 규칙이 있으면 지적하라.
```

---

## P10. 플러그인으로 포장 (선택)

```
security-barrier 를 사내 배포 가능한 Claude Code 플러그인으로 포장하라.

먼저 공식 문서에서 플러그인 매니페스트 구조와 marketplace 규격을 확인하라.
2차 출처(블로그)에 의존하지 말고 공식 문서 기준으로 작성하라.

포함할 구성요소:
- skills/  : security-barrier 스킬
- hooks/   : 사전 차단용 최소 훅
             (PreToolUse 로 axis 에 등록된 경로 접근 차단.
              훅의 차단 동작과 exit code 규약을 공식 문서에서 확인한 뒤 구현하라)
- commands/: 필요 시

버전은 semver 로 시작하고, axis 스키마 버전과 스킬 버전을 분리하라.
회사 정책이 바뀌면 axis 만 갱신되어야 한다.
```

---

## 대화 기록 요약

이 프롬프트들이 나온 배경. 상세 근거는 PDF 참조.

| 턴 | 질문 | 결론 |
|---|---|---|
| 1 | 이미 있는 스킬인가? 논문은? | 동일 스킬 없음. 단 사전 차단(입력 DLP)은 포화. 사후 추적·폐기가 빈 공간. "LLM이 잊게 하기"는 스킬로 불가 |
| 2 | 가중치 낮추고 dummy로 희석하면? | 기각. 조작할 핸들 없고, 희석은 정보를 제거하지 않음. 대안으로 CURE식 출력 교정 + 카나리 채택 |
| 3 | 스킬에 알고리즘을 넣을 수 있지 않나? | 가능. 스킬은 디렉터리이고 스크립트 번들 가능. 실행 결과를 모델 도달 전 주입할 수 있어 결정론적 1차 판정이 구조적으로 강제됨 |

### 확정된 설계

```
[Security Axis]  서명 + 읽기전용 + 인가자만 수정
   |
   +- GATE      사전 차단 (최소 구현. 기존 스킬과 중복 영역)
   |
   +- BREACH    유입 감지 시
        +- REVERSE TRACE  어디서 들어왔나   \  병렬
        +- FORWARD TRACE  무엇을 낳았나     /
        +- PURGE      파생물 -> 원본, tombstone
        +- SUPPRESS   CURE식 출력 교정 (완화로만 보고)
        +- VERIFY     재탐지 0건 + 해시 대조
        +- ROTATE     크리덴셜은 삭제 불가 전제
        +- REPORT     도달 불가 영역 항상 명시
```

### 미결 사항

1. 층위 A(사전 차단) 포함 범위 — 권고: 최소 훅만
2. Security Axis 등급 체계 — 회사 실제 등급을 쓸지, 일반 4단계 초안으로 갈지
